"""
UBUNTU

1. copy local file to remote server or vice-ver-sa --- scp
2. Count the items in directory --- ls -l | wc -l
3. Filter files by extension --- grep
4. Count files by extension --- ls -lR *.cfg | wc -l
5. Create symlink
6. setup crontab to automate script run
7. Install deb package
"""